/[Ll]ibrary/
/[Tt]emp/
/[Obj]/
/[Build]/
/[Builds]/
/[Logs]/
/[MemoryCaptures]/

# 에셋 메타데이터와 설정 파일은 AI가 읽어야 하므로 제외하지 않음
# *.meta (삭제됨)
# *.asset (삭제됨)
# *.unity (삭제됨)
# *.prefab (삭제됨)

# 솔루션 및 프로젝트 파일 (코드 구조 파악용)
!.csproj
!.sln

# 유니티 자동 생성 파일 중 불필요한 것들만 제외
*.swp
*.bak
*.tmp
.DS_Store
DerivedData/






